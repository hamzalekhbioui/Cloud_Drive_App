| Phase | Task | Priority / complexity | Likely components | Why / dependency / expected result |
| --- | --- | --- | --- | --- |
| 1 — Critical fixes | Remove SAS from public metadata and token from recipient DTO; add a permission-aware authenticated recipient stream | P0 / Medium | `ShareService`, `ShareController`, share DTOs, shared/public pages, tests | Stops unauthorized downloads. Do before any public deployment. |
| 1 — Critical fixes | Centralize file policy and enforce team roles per action | P1 / Medium | `FileService`, `TeamService`, repositories, controllers, tests | Prevents destructive BOLA. Requires a written action matrix. |
| 1 — Critical fixes | Rotate the Azure credential and implement secret/CI scanning | P1 / Low | Azure portal/deployment config, `.gitignore`, workflow | Prevents cloud-account compromise. Must happen immediately outside code. |
| 2 — Core file lifecycle | Create a validated upload-start DTO; add pending expiry/cancel worker and blob/DB compensation records | P1 / High | Upload DTO/service/repository/migration/storage/job/tests | Restores quota and removes orphans after interruption. Depends on deciding lifecycle states. |
| 2 — Core file lifecycle | Implement real trash retention or remove the claim | P1 / Medium | Repository query, scheduled job, storage, UI, tests | Makes data retention truthful and predictable. Decide whether trash counts against quota. |
| 2 — Core file manager | Add a folder entity and basic rename/move; then server-side paged list/search/sort | P1 / High | Migrations, file/folder services/controllers, frontend navigation | Converts flat storage into a credible drive. Do not add nested folders before ownership/team policy is stable. |
| 3 — Security/reliability | Verify direct-upload bytes, set storage headers server-side, and quarantine/scan if available | P1 / High | Azure storage service, processing status, UI | Ensures actual stored content matches policy. Depends on lifecycle states. |
| 3 — Security/reliability | Add reset, verification, account deletion, and document the token strategy | P1 / High | Auth/user/email provider/frontend | Completes account lifecycle. Select an email provider before implementation. |
| 3 — Security/reliability | Harden proxy/CORS/401 handling and admin paging | P2 / Medium | Security/rate-limit/admin config/tests | Makes deployment behavior predictable. Depends on target hosting topology. |
| 4 — Testing/CI | Pin the JDK, fix Mockito setup, repair the isolation assertion, and add a workflow | P1 / Medium | `pom.xml`, test config, `.github/workflows` | Restores a real merge gate. Choose JDK 17 or 21. |
| 4 — Testing/CI | Add Testcontainers PostgreSQL/Azurite and critical end-to-end security tests | P1 / High | Test dependencies, integration tests | Validates the actual migration/storage protocol, not mocks alone. |
| 5 — Performance | Add indexes, page limits, projections/batch queries; change reconciliation to batched aggregation | P2 / Medium | Migrations, repositories, services | Needed once lists exceed hundreds or thousands of files. Depends on page/search API design. |
| 5 — Performance | Retire the legacy multipart endpoint after the migration period | P2 / Medium | API/frontend/docs/tests | Eliminates the duplicate, heap-heavy path. Only after direct uploads are robust. |
| 6 — Portfolio polish | Write the README, diagram, ADRs, demo instructions, and show green CI | P1 / Medium | Docs, workflow, demo assets | Demonstrates system design and engineering maturity without scope bloat. |
